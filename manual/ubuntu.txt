
You can remove a directory by writing:
-- rm -fr <filename>
-- rm -r <filename>

Uninstall programs by running:
-- sudo apt remove - <program>

use different shell
-- "type command" chsh
-- "specify shell" /usr/bin/("shell e.g. zsh")

access wsl "Ubuntu" through windows
-- "type \\wsl$\" into Fileexplorer address bar
